/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ['avatars.githubusercontent.com', 'raw.githubusercontent.com', 's.sde.globo.com', 's.glbimg.com'],

  },
};

export default nextConfig;
