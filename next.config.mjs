/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ['avatars.githubusercontent.com', 'upload.wikimedia.org', 'raw.githubusercontent.com', 'flagcdn.com', 's.sde.globo.com', 's.glbimg.com'],

  },
};

export default nextConfig;
